export class Ball {
x:number;
y:number;
r:number;
color:number;
vx:number;
vy:number;
}

// { x: getRandX(), y: getRandY(), r: radius, color: 
   // getRandomInt(30,330), vx: getRandVel(), vy: getRandVel() };